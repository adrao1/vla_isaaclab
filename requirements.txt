# Runtime packages pinned for Isaac Sim 4.5.0.0 / Isaac Lab v2.0.2.
# Create a developer-owned Conda environment and install these by following
# README.md. Do not install into a system Python.
#
# Isaac Sim wheels are hosted by NVIDIA. The CUDA 12.1 PyTorch wheel used by
# Isaac Lab v2.0.2 is hosted by PyTorch.
--extra-index-url https://pypi.nvidia.com
--extra-index-url https://download.pytorch.org/whl/cu121

# Core simulator, extensions, and cached Kit extensions.
isaacsim[all,extscache]==4.5.0.0

# Isaac Lab v2.0.2-compatible runtime versions used by this project.
torch==2.5.1+cu121
numpy==1.26.4
pillow==11.0.0
h5py==3.16.0

# Runtime dependencies declared by the shared Isaac Lab v2.0.2 packages. The
# packages themselves are linked from ISAACLAB_ROOT as documented in README.md.
onnx==1.16.1
prettytable==3.3.0
toml
tomli
hidapi==0.14.0.post2
gymnasium
trimesh
pyglet<2
transformers
einops
warp-lang
starlette==0.46.0
protobuf>=3.20.2,<5.0.0
hydra-core
tensorboard
moviepy

# LeRobot v3 is the default writer. Install lerobot==0.4.3 separately with
# --no-deps as documented in README.md: its generic dependency constraints
# conflict with the Isaac Sim 4.5-compatible Torch/Torchvision pins above.
# The independent v2.1 exporter remains selectable with --lerobot-version 2.1.
datasets==4.1.1
pyarrow==25.0.1
pandas==2.3.3
av==15.1.0
jsonlines==4.0.0
accelerate==1.15.0
torchvision==0.20.1+cu121

# Isaac Lab source is not installed from this repository. Follow README.md to
# verify the shared v2.0.2 checkout and link it into the user's environment.
